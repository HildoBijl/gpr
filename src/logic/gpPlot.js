// TODO: make a class that takes a settings object, including a gp object, as its parameter. It then visualizes a GP. When the "update" property is called, it updates its predictions based on the shown gp.

export default class GP {
	constructor(settings) {
		// TODO
	}
}